# typescript-astro-react-google-cloud

pnpm init

pnpm add --save-dev --save-exact @biomejs/biome

mkdir packages

touch pnpm-workspace.yaml

```
packages:
  - 'packages/*'
```

cd packages

pnpm create astro@latest

